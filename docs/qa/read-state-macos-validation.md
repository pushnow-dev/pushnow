# Read-state macOS execution evidence

2026-09-13. Run `python3 scripts/qa/run-read-state-macos.py` from this checkout. No Simulator, UI automation, or whole-app build is involved.

The script creates an isolated temporary Swift 6 package targeting macOS 14, copies production `InboxViewModel` (excluding unrelated ViewModels), `SecureV2Models`, `NotificationHistoryCache`, `EncryptedNotificationCache`, JSON codecs and data models, and executes the existing `InboxReadStateTests.swift` and `HomeRefreshTests.swift` with XCTest. Source SHA-256 values are saved in the generated package's `source-hashes.json` so the exact version tested is reviewable.

The final run at 13:10:29 executed **14 tests with 0 failures**, exit 0. Log: `/tmp/pushnow-read-state-macos-final.log`; package: `/var/folders/tl/g7dv9txs2ql62nwl63m017g40000gn/T/pushnow-read-state-macos-n87dg81t`. Production `HomeRefreshSchedule.swift` is also copied without modification. The six refresh tests verify all five eligibility gates, the 30-second interval constant, completion-before-delay order, stop on sleep cancellation, coalesced requests, account-generation rejection of old replies, explicit cancellation, and URLError.cancelled preserving idle or loaded history. The clock delay is injected for deterministic execution; this is not a physical-device 30-second wall-clock observation.

Coverage: all three message protocols leave the unread filter but remain in all; stale responses do not reverse reads; reads/deletions during initial load and refresh preserve unrelated new results; reset clears per-account view state; legacy/v1 IDs do not mutate colliding v2 details; old v2 deletion events still remove attachments; encrypted read ledger works before any snapshot exists and survives cache reconstruction plus a later stale snapshot; other users and devices remain unread.

Execution boundary: actual Foundation/Observation, AES-GCM CryptoKit encryption, authenticated file reads/writes, and production state/cache algorithms execute. Test keys are in memory; real OS keychain, authentication, networking, relative-date formatting, color presentation, and current environment selection are stubbed. SecureKeyStore deletion lookup returns an empty set, so this run tests deletion event compatibility but does not independently validate keychain tombstone persistence. Reconstructing a cache with the same injected key tests disk persistence across instances, not OS process/keychain relaunch. Physical-device integration remains separate. No production algorithm is rewritten in the harness.
