# Pushnow release QA — 2026-09-13

## Scope and hardware

Inspected the actual iOS app, not a web mockup. Bundle `com.createitv.pushnow`, marketing version 1.0, build 1; project/scheme remain `JiZhi`. Development team `677U99F8TX`.

- Baseline: iPhone 16 Pro, iOS 18.4, UDID `38EEDBF7-0D38-4496-A62C-1D5CA5D8CCD2`.
- Physical: iPhone 15 Pro, iOS 18.7, paired over local network, developer mode enabled, DDI available. CoreDevice ID `863FF34A-1D38-5EBF-8C8A-4F3EBFF8A6DB`.

## Executed verification

- Debug simulator build succeeded, installed, launched, and inspected through Simulator UI.
- Home, email-login form, Settings, Membership, Discover and Reminders navigation exercised. Logged-out screens correctly require authentication; no authenticated backend flow is claimed from those checks.
- Light home/login/membership and dark home/discover inspected. Text remains readable and principal controls fit the device. Neutral backgrounds align with the design supplement, but blue tab/control accents remain.
- Membership shows Free/Plus/Pro comparison, monthly purchase selection, restore and redeem entries, legal links. In the logged-out simulator products are Unavailable and purchase/restore controls disabled. No purchase or restore success is claimed.
- Existing physical-device UI was visually inspected via iPhone Mirroring: actual weather notification and previous real notification entries were visible. This is not yet a native screenshot deliverable.
- Swift source audit: largest file 267 lines, no Swift file over 350 lines; primary views are decomposed.

## Initial blockers found

- English launch displayed Chinese authentication empty-state title/body in Home and Reminders. Reported to coordinator; separate agent is repairing localization.
- Localizable.xcstrings initially had 298 keys with English/Chinese coverage, but Japanese, Korean, Spanish and German each missed 52 keys. Source language was zh-Hans rather than English. Reported for repair.
- First physical XCTest build needed command-line `DEVELOPMENT_TEAM=677U99F8TX` because UITest target lacks team. Signed build then completed, but launch blocked on `Unlock 林逍遥 to Continue` (device locked).
- Signed-device screenshot XCTest is prepared for six languages; no raw physical screenshots are claimed before XCTest attachments exist.
- Full authenticated simulator registration/login/verification/session/resource isolation and purchase sandbox flows are not yet verified this turn.

## Evidence

- Simulator build: `/tmp/pushnow-qa-sim-build.log`.
- Physical signed build/test: `/tmp/pushnow-device-review-signed.log`.
- Six-language screenshot run: `/tmp/pushnow-device-sixlocale.log`.
- Actual simulator PNGs: `metadata/screenshots/raw-simulator/`.
- Screenshot test: `JiZhi/UITests/AccountFlowUITests.swift`, `ReviewScreenshotUITests`.

This is an interim report; submission readiness has not passed while these gates remain open.

## Native screenshot fallback attempted

Xcode Devices > Take Screenshot successfully wrote a PNG to Desktop while the phone was locked, but visual inspection confirmed an entirely black image. It is unsuitable for marketing or review and was not copied into the raw-device deliverables. Physical unlock is still required. The six-locale signed XCTest process waits on this condition.

## Unlocked retry and screen-capture status

After the user unlocked the phone, CoreDevice reported `passcodeRequired=false` and `unlockedSinceBoot=true`. `test-without-building` launched the signed XCTest Runner, but failed before test bootstrap with IDE channel refusal and runner exit code 74. This is a device test transport failure, not an app test failure.

The production app then launched successfully in English through devicectl. iPhone Mirroring visibly showed English navigation plus the existing real messages. Xcode Devices > Take Screenshot produced 1179 × 2556 all-black RGB images. Quitting iPhone Mirroring and retrying once still produced all-black output, so retries were stopped. The phone must remain physically awake with Pushnow visible for the next native capture. No black image remains in the raw-device deliverable directory.

## Content localization repair (source-only)

The later user instruction prohibited Simulator and mirror interaction for this subtask. No build or UI operation was performed during this repair.

- Added `docs/qa/localization-content-entries.json`: 77 keys, each with English, Simplified Chinese, Japanese, Korean, Spanish and German. Coordinator merges these into the shared catalog.
- Discovery subscription copy now resolves through the selected-language catalog instead of a Chinese-only dictionary.
- Dynamic reminder mode, priority, delivery label, saving/creating/acknowledging buttons now use explicit locale lookup. Relative delay uses `RelativeDateTimeFormatter`, removing the Chinese suffix concatenation.
- Reminder editor timezone label, DatePicker and API timezone now agree with the selected app timezone.
- Remote model fallback labels, source state, category, delivery/priority modules, and reminder fallback text are localized. User-supplied names, titles and summaries remain verbatim.
- Reminder date/time labels use selected locale/timezone; grouping preserves repository order instead of sorting by hardcoded Chinese sample dates.
- Legacy detail displays the actual item's summary instead of a hardcoded Q2 report paragraph, and does not invent a tonight-at-20:00 reminder.
- All edited Swift files remain at or below 212 lines. JSON completeness and `git diff --check` passed. Compilation and physical-device rendering remain for coordinator verification.
