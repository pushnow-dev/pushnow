# PushNow AEO content and indexing readiness — 2026-09-13

## Delivered

Six localized public `/guide` pages (English, Simplified Chinese, Japanese, Korean, Spanish, German) explain the actual automation-to-inbox workflow. Each has an introduction, four setup steps, and six substantial answers: supported results, acceptance versus device delivery, quiet inbox use, encryption metadata boundaries, purchases/restoration, and integration troubleshooting. Each homepage now links to its localized guide and surfaces two answers directly. The work favors useful explanations over keyword repetition.

The FAQPage JSON-LD is generated from the same data as the visible questions and answers. All six guides have self-canonical links and seven alternates including x-default. The sitemap now includes the documentation index and documentation articles as well as the guides: 121 unique public URLs, versus the previous 30 URLs. All targets exist in the generated build. Account dashboards are excluded from the sitemap and have `noindex, follow`.

Removed unsupported numeric Offer markup and the circular downloadUrl pointing to the website's own download anchor from the homepage application schema. Removed invalid FAQPage typing without Question entities from support pages. Added og:site_name. Added a concise llms.txt linking to the same human-readable public content; it is an optional navigation aid, not a ranking requirement or guaranteed citation mechanism.

## Verification

- `npm --prefix web run check`: 0 errors, 0 warnings, 0 hints across 53 files.
- `npm --prefix web run build`: success, 127 generated pages.
- Parsed six built guide pages: each has six JSON-LD answers, each exactly present in the visible page text, one canonical and seven language alternates.
- Parsed XML sitemap: 121 unique URLs; every target maps to a built index.html.
- Checked all six generated dashboard pages for noindex.
- Build still emits existing HPKE dependency browser `crypto` externalization warnings. These are not proof of broken browser execution, nor has this task independently revalidated the account console crypto flow.

## Product evidence and limits

Content checked against `web/src/docs/en/hpke-authorization.md` and the current documentation structure. Discovery source `JiZhi/Views/Discover/DiscoverView.swift` and `DiscoverySubscriptionEditor.swift` explicitly says saved locally / pending activation. The new guide does not claim automatic third-party subscriptions.

The release coordinator confirmed CNY 8 / CNY 18 monthly products across 175 storefronts. The homepage examples are retained, with a localized purchase caveat directing users to the actual in-app and App Store confirmation price. The broader original homepage is still primarily English outside the Chinese locale; the new guide content and homepage guide blocks are fully localized. Corrected six historical English/Chinese APNs statements in `web/src/data/site.ts`: sending and delivery records are implemented; device-visible delivery still requires actual-device checks.

No App Store availability, real-device delivery, live RevenueCat entitlement, Search Console ownership, index inclusion, AI citation, or ranking result is claimed by this task.

## Deployment and follow-up

The coordinator confirmed physical-device QA, and production deployment/readback was completed on 2026-09-13. See `aeo-deployment-2026-09-13.md` and its JSON evidence. For future changes, deploy from `web/` with `npx wrangler deploy --config wrangler.jsonc` after a fresh build. Existing Worker name is `jizhi-web`; do not rename the production service casually. Then fetch production guide pages, sitemap.xml, robots.txt, and llms.txt, confirm canonicals and JSON-LD, and inspect desktop/mobile rendering. The downloaded browser representation, not a deployment success message, is the production evidence.

Search Console URL Inspection and sitemap submission require an authenticated property. Track a baseline query set (PushNow iPhone automation notifications, PushNow encrypted inbox, PushNow sender authorization, PushNow restore purchases, and localized equivalents). Record date, engine, query, cited URL and screenshot; do not infer visibility gains from source changes alone.

Google's current guidance says ordinary SEO foundations apply to AI features, useful text should be accessible, and structured data should match visible text. No special AI file or schema guarantees inclusion. Source verified in this task: https://developers.google.com/search/docs/appearance/ai-features .

## Changed files

- `web/src/data/product-guide.ts`
- `web/src/sections/ProductGuidePage.astro`
- `web/src/pages/guide.astro`
- `web/src/pages/[lang]/guide.astro`
- `web/src/sections/HomePage.astro`
- `web/src/sections/LegalPage.astro`
- `web/src/layouts/SiteLayout.astro`
- `web/src/pages/sitemap.xml.ts`
- `web/public/llms.txt`
- this report
